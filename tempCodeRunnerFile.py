        # Draw the frequency of the current key on the bottom right of the screen
                            font = pygame.font.SysFont('Calibri', 20, True, False)
                            screen.blit(font.render(str(freq